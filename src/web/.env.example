# Estate Kit Frontend Environment Configuration
# This file serves as a template for required environment variables.
# Copy this file to .env and update values according to your environment.

# Application Environment
# Required: true
# Valid values: development, staging, production
VITE_APP_ENV=development

# API Configuration
# Required: true
# Format: Must be a valid HTTPS URL with environment-specific subdomain
# Example domains: 
# - Development: api-dev.estatekit.ca
# - Staging: api-staging.estatekit.ca
# - Production: api.estatekit.ca
VITE_API_URL=https://api-dev.estatekit.ca

# Auth0 Authentication Configuration
# Required: true
# Note: All Auth0 values must match your Auth0 tenant configuration

# Auth0 Domain
# Format: Must be a valid Auth0 tenant domain
# Example: dev-estatekit.auth0.com
VITE_AUTH0_DOMAIN=dev-estatekit.auth0.com

# Auth0 Client ID
# Format: Must be a valid Auth0 application client ID
# Warning: Never commit actual client IDs to version control
VITE_AUTH0_CLIENT_ID=AUTH0_CLIENT_ID_PLACEHOLDER

# Auth0 API Audience
# Format: Must match the API Gateway URL or configured audience in Auth0
# Should align with VITE_API_URL domain
VITE_AUTH0_AUDIENCE=https://api-dev.estatekit.ca

# Auth0 Redirect URI
# Format: Must be a valid HTTPS URL matching the Auth0 application configuration
# Must be registered in Auth0 application settings
# Example domains:
# - Development: app-dev.estatekit.ca
# - Staging: app-staging.estatekit.ca
# - Production: app.estatekit.ca
VITE_AUTH0_REDIRECT_URI=https://app-dev.estatekit.ca/callback

# Intercom Integration Configuration
# Required: true
# Format: Must be a valid Intercom application ID
# Warning: Never commit actual Intercom app IDs to version control
VITE_INTERCOM_APP_ID=INTERCOM_APP_ID_PLACEHOLDER

# Security Warnings:
# 1. Never commit .env file to version control
# 2. Keep production credentials secure and separate
# 3. Regularly rotate production credentials
# 4. Use different values for each environment
# 5. Validate all environment variables on application startup